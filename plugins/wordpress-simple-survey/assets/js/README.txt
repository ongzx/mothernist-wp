See http://codex.wordpress.org/Function_Reference/wp_enqueue_script before tinkering with jQuery. 

Some theme designers and plugin authors make mistakes when loading JS libs.

This plugin loads jQuery using the WordPress API (the correct way), you may need to contact your theme designer if you have multiple jQuerys loaded.
